# ============================
# TryggResa Backend Environment
# ============================

# Server port
PORT=4000

# MongoDB Connection String (example)
# Replace <username>, <password> and cluster-url
MONGODB_URI="mongodb+srv://<username>:<password>@cluster-url.mongodb.net/tryggresa"

# JWT Secret for authentication
JWT_SECRET="replace-with-your-secret-key"
